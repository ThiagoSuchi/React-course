const ExecutarFunction = ({ myMessage }) => {
  return (
    <div>
        <button onClick={myMessage}>Clique aqui!</button>
        <p>Olhe no devTools</p>
    </div>
  )
}

export default ExecutarFunction